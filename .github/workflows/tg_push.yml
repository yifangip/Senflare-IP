name: Telegram Push Senflare IP

on:
  workflow_dispatch:   # 手动触发
  schedule:
    - cron: "0 */6 * * *"   # 每6小时运行一次

jobs:
  push_to_tg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 仓库
        uses: actions/checkout@v4

      - name: 下载最新文件
        run: |
          curl -L -o Senflare-Pro.txt https://raw.githubusercontent.com/yifangip/Senflare-IP/refs/heads/main/Senflare-Pro.txt

      - name: 检查文件变化并推送 Telegram
        run: |
          # 计算新文件哈希
          NEW_HASH=$(sha256sum Senflare-Pro.txt | awk '{print $1}')
          OLD_HASH=$(cat .last_version.txt 2>/dev/null || echo "")

          if [ "$NEW_HASH" != "$OLD_HASH" ]; then
            # 文件名带北京时间
            TIME_STR=$(date -d "+8 hour" '+%Y-%m-%d_%H-%M')
            READABLE_TIME=$(date -d "+8 hour" '+%Y-%m-%d %H:%M')
            NEW_NAME="${TIME_STR}_优选ip.txt"
            mv Senflare-Pro.txt "$NEW_NAME"

            # 统计 IP 数量
            IP_COUNT=$(wc -l < "$NEW_NAME" | tr -d ' ')

            # Telegram 推送（单行 caption，避免 YAML 错误）
            curl -F document=@"$NEW_NAME" \
                 -F caption="📦 Senflare 优选IP更新通知 🕓 更新时间：$READABLE_TIME 📊 IP数量：$IP_COUNT 个 🧾 文件名：$NEW_NAME" \
                 "https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendDocument?chat_id=${{ secrets.TG_CHAT_ID }}"

            # 保存新哈希供下次对比
            sha256sum "$NEW_NAME" | awk '{print $1}' > .last_version.txt

            # 提交到仓库
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add .last_version.txt
            git commit -m "update last_version.txt (auto)" || true
            git push || true

            echo "文件已更新并推送 Telegram"
          else
            echo "文件未更新，不推送"
          fi
