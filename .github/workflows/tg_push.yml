name: Telegram Push Senflare IP

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */9 * * *"  # 每9小时运行一次

jobs:
  push_to_tg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 仓库
        uses: actions/checkout@v4

      - name: 下载最新文件
        run: |
          curl -L -o Senflare-Pro.txt https://raw.githubusercontent.com/yifangip/Senflare-IP/refs/heads/main/Senflare-Pro.txt

      - name: 对比文件哈希
        id: compare
        run: |
          NEW_HASH=$(sha256sum Senflare-Pro.txt | awk '{print $1}')
          OLD_HASH=$(cat .last_version.txt 2>/dev/null || echo "")
          if [ "$NEW_HASH" != "$OLD_HASH" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: 生成时间戳文件名与统计IP数量
        if: steps.compare.outputs.changed == 'true'
        id: info
        run: |
          # 北京时间 UTC+8
          TIME_STR=$(date -d "+8 hour" '+%Y-%m-%d_%H-%M')
          READABLE_TIME=$(date -d "+8 hour" '+%Y-%m-%d %H:%M')
          NEW_NAME="${TIME_STR}_优选ip.txt"
          mv Senflare-Pro.txt "$NEW_NAME"

          IP_COUNT=$(wc -l < "$NEW_NAME" | tr -d ' ')
          echo "file_name=$NEW_NAME" >> $GITHUB_OUTPUT
          echo "ip_count=$IP_COUNT" >> $GITHUB_OUTPUT
          echo "time_text=$READABLE_TIME" >> $GITHUB_OUTPUT

      - name: 推送到 Telegram
        if: steps.compare.outputs.changed == 'true'
        env:
          BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: |
          FILE_NAME="${{ steps.info.outputs.file_name }}"
          IP_COUNT="${{ steps.info.outputs.ip_count }}"
          TIME_TEXT="${{ steps.info.outputs.time_text }}"

          CAPTION="📦 Senflare 优选IP更新通知
🕓 更新时间：${TIME_TEXT}
📊 IP数量：${IP_COUNT} 个
🧾 文件名：${FILE_NAME}"

          curl -F document=@"$FILE_NAME" \
               -F caption="$CAPTION" \
               "https://api.telegram.org/bot${BOT_TOKEN}/sendDocument?chat_id=${CHAT_ID}"

      - name: 保存哈希
        if: steps.compare.outputs.changed == 'true'
        run: sha256sum "${{ steps.info.outputs.file_name }}" | awk '{print $1}' > .last_version.txt
